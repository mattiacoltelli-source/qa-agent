name: QA Agent — smoke tests

on:
  workflow_dispatch:
    inputs:
      run_write_tests:
        description: "Esegui anche i test @write (scrivono su Supabase/librerie reali)"
        type: boolean
        default: false

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Installa dipendenze
        run: npm install

      - name: Installa i browser Playwright (chromium)
        run: npx playwright install --with-deps chromium

      - name: Esegui la suite (sola lettura)
        if: ${{ github.event.inputs.run_write_tests != 'true' }}
        run: npm test

      - name: Esegui la suite (inclusi i test @write)
        if: ${{ github.event.inputs.run_write_tests == 'true' }}
        run: npm test
        env:
          RUN_WRITE_TESTS: "true"

      - name: Carica il report HTML
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: reports/html
          retention-days: 14

      - name: Carica i risultati JSON
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: results-json
          path: reports/results.json
          retention-days: 30
