// Spot (repo "Spot", cartella storica "vacanza"): nessun backend, solo
// localStorage per client — niente da controllare a livello dati, solo che
// l'app sia raggiungibile.

export const label = "Spot";
export const url = process.env.VACANZA_BASE_URL ?? "https://mattiacoltelli-source.github.io/Spot/";
